use flake
# Load secrets from secretspec into the dev shell.
# Provider/profile come from your global config (~/.config/secretspec/config.toml).
watch_file secretspec.toml
direnv_load secretspec run -- direnv dump
